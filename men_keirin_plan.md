# 男子競輪への拡張 — 方式設計とタスクリスト

ガールズケイリン予測AI（[girls_keirin_ai_spec.md](girls_keirin_ai_spec.md)）の基盤を男子競輪へ広げるための設計。
2026-08-10 起案。**Phase 0（実測による前提確認）の結果が出るまで Phase 2 以降の設計は確定しない。**

## 0. 前提と方針

- ガールズで確立した運用ルールは男子でも維持する。特に **事前に採否基準を宣言し、外れたら打ち切る**。
  データ量が10倍になると偶然の有意差を拾いやすくなるため、規律はより重要になる。
- **収益は目的にしない。** ガールズでは全手法で ROI<100%（控除率25%＋favorite-longshot bias＋効率的市場）。
  男子は投票額がさらに大きく市場効率も高いと考えられる。作るのは「較正された予測AI」。
- ガールズの検証結果は男子に自動的には転移しない。バンク交互作用など採用済みの特徴も**男子では再検証**する。

## 1. 現状の棚卸し（2026-08-10 実コード調査）

### そのまま流用できる

| 項目 | 根拠 |
|---|---|
| 三連単の確率計算 | `src/model/plackett_luce.py` の `all_trifecta_probs` は `permutations(riders,3)`。**車立て非依存**で9車=504通りも自動対応。「210」は docstring のみ |
| 収集パイプライン | `src/collect/dataset.py: collect_race_dataset(..., require_girls=False)`。**引数であってハードコードではない** |
| 出走表パーサ | `class_rank` / `leg_type` / `gear_ratio` / `racing_score` を既に取得 |
| 並び予想パーサ | `src/collect/gamboo_racecard.py: parse_narabi`。隊列＋脚質を抽出済み。**男子では中核特徴になる** |
| DBスキーマ | races / entries / results / odds_final_trifecta / payouts_trifecta / narabi は男女共通。`races.is_girls` で分離可能 |
| モデル基盤 | lambdarank・Elo・walk-forward・バケット分析・EVエンジンは流用可 |

### 要対応

| 項目 | 現状 | 対応 |
|---|---|---|
| 開催格 | 実DBに `races.grade` 列が**無い**（schema.sql には定義あり＝未適用） | スキーマ移行。男子はF2〜G1で強度差が大きく必須 |
| 級班 | `class_rank` は `L1` のみ（45,625件すべて） | S1/S2/A1/A2/A3 の階層を特徴化 |
| 脚質 | 「両」82% / 追13% / 逃5%（ガールズ特有） | 男子は逃・捲・差・マークが明確に分離 |
| 車立て | 5車69R / 6車353R / 7車6,166R | **9車対応**。組合せ504点でUI・EV・参考買い目に波及 |
| 「210」の散在 | config / api / results_history / tests | 車立てから導出する定数へ |

## 2. 男子とガールズの構造的な違い

| 観点 | ガールズ | 男子 | 影響 |
|---|---|---|---|
| **ライン** | 概念が薄い（記者の予想先頭が実際にバックを取る率 ≒20%、実Sと一致34.2%） | **競走の中核**。同県・同地区で連携 | 最大の新規要素。並び予想の価値が桁違いの可能性 |
| 実力階層 | L級のみ | S1/S2/A1/A2/A3 | 級班をまたぐと競走得点のスケールが不連続 |
| 車立て | 7車固定 | 7車 / 9車 | 組合せ数・相対化・UI |
| 開催格 | ほぼ均質 | F2〜G1 | メンバー強度が大きく変動 |
| データ量 | 約6,600R | 約10倍 | 収集時間・学習時間・レート制限 |

## 3. 方式設計

### A. モデルは分離、コードは共有（推奨）

級班階層・車立て・ライン有無・決まり手構造が違いすぎるため、`is_girls` フラグを1モデルに足す形は不利。
`pl_model.pkl`（ガールズ）と `pl_model_men.pkl` を分け、特徴生成・学習・評価コードは共通化する。
既に `model.feature_names` 追従の作りなので、モデルごとに特徴セットを変えられる。

### B. DBは同居（推奨）

`races.is_girls` で分離でき、テーブル構造も共通。分けると選手マスタ・Elo・会場統計が二重管理になる。
データ量10倍にはインデックスで対応（WALは適用済み）。

### C. ライン特徴が最大の新規開発

並び予想から**ライン構造を復元**し、ライン単位の特徴を作る。候補:
ライン長 / ライン内位置（先頭・番手・3番手）/ ライン全体の実力 / 番手選手の質 / 単騎か否か / 同県・同地区の結束。
現在の並び特徴（`src/features/rider_narabi.py`）は隊列位置5列のみで**ライン概念が無い**。

### D. 9車対応は横断的

組合せ504、`combos` 配列サイズ、オッズテーブルUI、参考フォーメーション、`detect_missing_trifecta`。
影響範囲が広いので早期に定数化する。

## 4. Phase 0 で先に測ること（仮説で設計しない）

| 測ること | 判断への効き方 |
|---|---|
| **男子の並び予想の的中率**（予想先頭 vs 実S / 実B） | ガールズは20%。男子が60%ならライン特徴は主力、30%なら期待を下げる。**ライン特徴の設計はこの数字で決まる** |
| 級班別の決まり手・実力分離度 | 級班を特徴にするか、モデルを級班でも分けるかの判断 |
| 9車と7車の構造差 | 同一モデルで扱うか分けるか |
| 既存パーサが男子ページで通るか | 収集コストの見積り |

**Phase 0 は本番DBに書かない。** 判断材料を集める段階なので、プローブ用の別DB
（`data/keirin_men_probe.sqlite`）へ収集し、方針が固まってから Phase 1 で本収集する。

## 4.5 Phase 0 の実測結果（2026-08-10, 男子150R / プローブDB）

### 判断: **ライン特徴に投資する価値がある（明確にYES）**

**① 並び予想の的中率 — 男子はガールズの2.4倍**

| | 男子（150R） | ガールズ |
|---|---|---|
| 予想先頭が実B（主導権）と一致 | **48.0%** | 20% |
| 予想先頭が実S（初手先頭）と一致 | 30.8% | 34.2% |

**構造が逆転している。** ガールズは S(34.2%) > B(22.1%) で「並び予想は初手寄り」だったが、
男子は **B(48.0%) > S(30.8%)** で「並び予想は主導権を取る隊列の予想」として機能する。

**② 実B取得者は並び予想の何番目にいたか — ライン構造の証拠**

```
index0: 48.0%  ← 予想先頭（1ライン目の先頭）
index1:  2.7%  ← 番手。谷になる（当然）
index2: 12.0%  ┐
index3: 18.7%  ├ 2〜3ライン目の先頭
index4:  8.7%  ┘
```
index1 が谷で 2〜4 に山がある＝**ラインの先頭がBを取る**構造がそのまま出ている。

**③ 並び予想の脚質別 B取得率 — 主導権はほぼ脚質で決まる**

| 脚質 | B取得率 |
|---|---|
| 先行 | **51.9%** (68/131) |
| 押え先 | **34.5%** (68/197) |
| 自在 | 9.2% (12/130) |
| 追込 | **0.2%** (1/593) |
| 追上 | 0.0% (0/44) |

追込・追上は637名中1名しかBを取っていない。ガールズは `leg_type` が「両」82%で分離
できなかったが、**男子は `narabi.leg` が明確に分離する**。

**④ 推定ライン本数（自力型の人数）**: 7車は2本(59R)・3本(29R)、9車は3本(15R)・2本(12R)が典型。

**⑤ 決まり手が真逆**

| | 逃 | 捲 | 差 |
|---|---|---|---|
| 男子（150R） | 19.3% | 30.0% | **50.0%** |
| ガールズ（6,566R） | 21.4% | **46.6%** | 31.9% |

ガールズは捲りが半分、男子は差しが半分。**モデル分離が正しいことの裏付け**。

**⑥ 級班**: A1/A2 55R・S1/S2 36R・A3 34R と混在レースが多く、級班特徴は必要。

**⑦ 収集レイヤは無改造で通る**: 150R 欠損ゼロ。9車504点・級班・並び予想すべて取得。

### 留意（この結果の限界）
- n=150・3日分。開催格や級班に偏りがある可能性。決まり手の差50%はA級中心の標本による可能性がある
- ライン本数は `narabi.leg` の自力型人数からの**推定**。正しくは並び予想HTMLのライン境界を使う（下記）

### Phase 0 で判明した最優先の実装課題
`parse_narabi` が**ライン境界を捨てている**。実HTMLはラインの切れ目を
`<span class="icon_p space"></span>` で明示しているが、数字を持たないため読み飛ばされ、
返り値が1本のフラットな隊列になる。男子ではライン構造が精度を決めるため、
**`lines: [[5,3,7],[4,1],[6,2]]` の保持と `narabi` テーブルへの `line_id`/`pos_in_line` 追加が最優先**。

## 4.6 ライン構造の実測（2026-08-11, 男子25,134R / 直近1年）

`data/keirin_men.sqlite`（19.5時間・25,170R収集・全レースにライン情報あり）での実測。

### ライン強度 × ライン内位置（7車立て・1着率 / 3着内率）

| ライン強度 | 先頭 | 番手 | 3番手 | 単騎 |
|---|---|---|---|---|
| **1位（最強）** | **33.2% / 66%** | 22.4% / **65%** | 3.7% / 44% | 18.6% / 47% |
| 2位 | 18.8% / 48% | 14.0% / 46% | 2.4% / 27% | 9.7% / 36% |
| 3位 | 11.3% / 33% | 8.0% / 30% | 1.8% / 21% | 4.8% / 20% |

n: 1位 先頭18,797・番手18,718・3番手9,896・単騎2,310 ／ 3位 単騎10,609 など。

**読み取れる構造:**
1. **1着は「強いラインの先頭」**。最強ライン先頭33.2%はランダム(14.3%)の2.3倍
2. **3着内では最強ラインの番手(65%)が先頭(66%)とほぼ同格** ＝ 紐として先頭と同価値。
   1着率では劣る(22.4% vs 33.2%)が3着内では並ぶ、という非対称性がガールズには無かった
3. **3番手はどのラインでも1着が無い**（1.8〜3.7%）。1着候補から外せる。
   ただし最強ラインなら3着内44%で紐候補にはなる
4. **強い単騎は侮れない**。最強ライン相当の単騎18.6%は2位ライン先頭18.8%と同等

### ライン内位置別（全車立て）

| 位置 | n | 1着率 | 3着内 | B取得 |
|---|---|---|---|---|
| 先頭（自力） | 63,754 | 20.8% | 48.5% | **37.8%** |
| 番手 | 63,438 | 15.3% | 47.1% | 0.4% |
| 3番手 | 28,211 | 2.7% | 32.3% | 0.1% |
| 4番手以降 | 3,134 | 1.0% | 15.9% | 0.0% |
| 単騎 | 18,070 | 7.8% | 27.3% | 3.8% |

主導権(B)はライン先頭がほぼ独占（37.8% vs 番手0.4%）。

### ライン長（1人あたりに正規化・7車立て）

**生の「1着輩出率」は人数が多いほど機械的に上がるので、1人あたりで見る必要がある。**

| ライン長 | 1人あたり1着率 | ランダム(14.3%)比 |
|---|---|---|
| 1名（単騎） | 7.7% | 0.54倍 |
| 2名 | 12.4% | **0.87倍（不利）** |
| 3名 | 17.4% | 1.22倍 |
| 4名 | 20.0% | 1.40倍 |
| 5名 | 19.0% | 1.33倍 |

**2名ラインはランダム期待値を下回る。** 有利になるのは3名以上。
（初期分析 n=77 では生の輩出率だけを見て「長いほど有利」と読んだが、正規化すると2名は不利）

### 隊列構成の分布
7車は**三分戦が最多**（11,746R・47%）、次いで二分戦（8,928R・36%）。
9車は四分戦(1,036R)と三分戦(1,220R)が中心。

## 4.7 級班によるライン決着の違い（2026-08-11, 25,116R）

ユーザー仮説「A3（チャレンジ戦）は弱いメンバーが多く、強い先頭の番手についても千切れて
ライン通りの決着にならないのでは」を検証した。**仮説は逆方向で否定されたが、実務的な核は成立する。**

### 千切れの直接検証 — 起きていない

| 級班 | 逃げ決着 | 先頭勝ち時の**番手2着率** | 番手が6着以下 |
|---|---|---|---|
| S1/S2 | 15.5% | 50.4% | 14.2% |
| S2 | 19.9% | 52.0% | — |
| A1/A2 | 25.0% | 53.1% | 13.2% |
| **A3** | **35.6%** | **53.3%** | 13.7% |
| 全体 | 24.4% | 52.1% | 13.8% |

A3の番手2着率は**全級班で最高**（S1/S2 比 +2.9pt、95%CIは重ならない）。
番手が6着以下に沈む率も全体と同じ。**千切れの痕跡が無い**。
ライン決着率（1-2着が同一ライン）も A3 60.1% で最高。

### ただし「番手の価値が級班で反転する」は強く成立

| 級班 | 先頭の1着率 | 番手の1着率 | 番手÷先頭 |
|---|---|---|---|
| S1/S2 | 15.4% | 16.9% | **1.09**（番手の方が強い） |
| A1/A2 | 21.4% | 15.6% | 0.73 |
| **A3** | **28.0%** | 13.0% | **0.47** |

**原因は千切れではなく「先頭の逃げ残り」。** A3の番手の3着内率は47.8%で S1/S2 の45.9%より
むしろ高く、比が崩れているのは分母の先頭が 41.3%→56.8% と激増しているため。
裏付けは逃げ決着率の差（A3 35.6% vs S1/S2 15.5% ＝ **+20.2pt**）と、
2着決まり手「ク」（番手が続いた形）が A3 45.4% で最高（S1/S2 33.8%）であること。

### 買い目への含意
- **A3**: 「ライン先頭を1着固定 → 番手を2着」の**順序を決め打ちする型**が最も報われる
  （先頭勝率62.0%・その番手2着53.3%）
- **S1/S2**: 番手が先頭を差すため、頭を決め打ちせず**番手も1着候補に置く**必要がある

同じ「強いラインの先頭と番手」でも級班で買い方が逆になる。
→ ライン特徴には**級班との交互作用が必須**（`src/features/line_features.py` の
`x_mate_class` / `x_head_class`）。級班はレース定数で順位モデル（シフト不変）には
そのまま効かないため、レース内で変動するライン内位置に掛けて注入する。

### 留意
A3はライン本数が少ない（3.14本 vs S1/S2 3.47本）ため、ライン決着率の生の高さには
構造的な押し上げが含まれる。構成を補正した lift では A3 が最低（1.86 vs 2.23）だが、
加法差では −1.3pt と小さく、「A3はライン決着になりにくい」は成立しない。

## 4.8 ライン特徴の検証結果（2026-08-11, 男子7車 20,554R・walk-forward 5fold）

基準は「拡張20特徴 + rel_elo」（21列）。ガールズ用にチューニングされた展開/並び特徴は
男子で意味が変わるため入れず、**ライン特徴8列の寄与だけを切り出した**。

| 指標 | 純増 | 勝ちfold |
|---|---|---|
| **top1** | **+2.03pt** | **5/5** |
| **tri10** | **+4.61pt** | **5/5** |
| logloss | −0.0506（改善） | 5/5 |
| ece | +0.00138（悪化） | 0/5 |

fold別: top1 41.9→43.9 / 41.9→43.5 / 43.7→45.8 / 42.5→44.9 / 39.8→41.9（全fold改善）

**事前基準（主・副とも）を充足。** 効果量は本プロジェクトで観測した中で最大:
- ガールズのバンク交互作用（採用済み）: tri10 +0.66〜0.92pt
- ガールズの逃げ切り率（不採用）: tri10 −0.26pt
- **男子のライン特徴: tri10 +4.61pt** ＝ 一桁違う

### 留意点
**較正（ece）は全5foldで悪化した**（+0.00138、改善0/5）。絶対値は 0.0044〜0.0069 →
0.0052〜0.0082 と小さいままだが、系統的な劣化でノイズではない。識別が大きく上がった
ぶん確率が過信気味になる典型パターン。ガールズ本番モデルの ece≒0.004 と比べると
男子は 1.7倍程度になるため、**本番投入時は較正の確認・必要なら較正段を検討する**。

**男子はガールズより本質的に難しい。** 基準の top1 が男子 41.9〜43.7% に対し
ガールズは 66〜69%。級班で実力が揃えられているぶん混戦度が高い。

## 4.9 男子の特徴セット決定（2026-08-11, 7車 20,554R・5fold）

段階的に足して、どの塊がどれだけ効くかを切り分けた。基準は「拡張20特徴 + rel_elo」。

| 案 | 特徴数 | top1 | tri10 | logloss | ece |
|---|---|---|---|---|---|
| A 基準（拡張20+Elo） | 21 | 41.96% | 28.78% | 1.5116 | 0.00573 |
| B +ライン8列 | 29 | 43.99% | 33.40% | 1.4610 | 0.00712 |
| C +脚質1列 | 30 | 44.00% | 33.11% | 1.4604 | 0.00850 |
| D +展開10（脚質込） | 40 | 44.07% | 33.68% | 1.4587 | 0.00842 |
| **E ライン8+展開10** | **39** | **44.31%** | **33.80%** | **1.4578** | 0.00810 |

**増分（基準Bとの直接比較）**

| 比較 | top1 | tri10 | 判定 |
|---|---|---|---|
| B − A（ライン8列） | +2.03pt (5/5) | **+4.61pt (5/5)** | 充足 |
| C − B（脚質1列） | +0.01pt (2/5) | **−0.28pt (0/5)** | **未充足＝不採用** |
| D − B（展開10・脚質込） | +0.08pt (2/5) | +0.28pt (4/5) | 充足 |
| **E − B（展開10・脚質なし）** | **+0.32pt (4/5)** | **+0.40pt (4/5)** | **充足** |

### 決定: 男子の特徴セットは **拡張20 + rel_elo + ライン8 + 展開10 = 39列**

- **ライン8列が主役**（tri10 +4.61pt・5/5）。ガールズのバンク交互作用(+0.66〜0.92pt)の5〜7倍
- **脚質1列は不採用**（tri10 −0.28pt・0/5）。ライン先頭≒先行/押え先、番手≒追込なので
  `ln_head`/`ln_mate` に既に吸収されている。実測でマッピングを正した意義はあったが冗長だった
- **展開10列はライン特徴の上にも純増する**（E−B で tri10 +0.40pt・4/5、top1 +0.32pt・4/5）。
  脚質を外した E が D を上回るのは、効かない列を抜いたぶん素直に効いたということ
- 並び5列（NARABI_KEYS）は使わない。`narabi_pos`/`narabi_lead`/`narabi_mid` はライン特徴に
  完全に包含され、`narabi_leg` は上記のとおり不採用

### 較正の課題（未解決）
ece が 0.00573（基準）→ 0.00810（案E）と悪化。識別が上がるほど過信気味になる典型で、
ガールズ本番（≒0.004）の2倍。**本番投入時に較正段（Platt/Isotonic）の検討が必要。**

### 既知の不具合（ガールズ側・未修正）
`rider_narabi.LEG_AGGR` が実データ語彙を網羅しておらず、**ガールズは33.9%が未定義**で
既定1.0（自在扱い）になっている（「追上」28.1%が主因）。実測B取得率は追上1.6%で、
本来は0が妥当。`narabi_leg`/`narabi_midleg` はガールズ本番38特徴モデルに入っているため
実害があるが、**直すと学習と推論で値が食い違う（skew）ので再学習とセットでしか直せない**。
男子は `line_features.LEG_AGGR_MEN`（実測ベース）を別に持たせて回避した。

## 4.10 9車立ての扱い（2026-08-11・決定）

### 決定: **7車で学習したモデルを9車にもそのまま適用する**（学習セットに9車は入れない）

同一期間・同一の9車テスト集合（1,396R）で3案を比較。fold境界は7+9車の統合indexで切り、
3案が完全に同じ窓で学習・評価されるようにした。

**9車テストでの評価（5fold平均）**

| 案 | top1 | tri10 | logloss | ece |
|---|---|---|---|---|
| 案1 7車のみ学習 | 33.56% | **18.34%** | 1.7417 | 0.01072 |
| 案2 7+9混在学習 | 33.66% | 17.01% | 1.7334 | 0.00927 |
| 案3 9車のみ学習 | 34.27% | 16.79% | 1.7498 | 0.01589 |

同一レースでのMcNemar検定（n=1,396）:
- 案2−案1 top1 +0.07pt（**p=1.000**＝完全に互角）
- 案2−案1 tri10 **−1.29pt（p=0.050・勝ち1/5fold）**＝弱いが悪化側に有意

**混在学習は上積みゼロで下振れリスクだけ**のため、学習は7車のみとする。

**7車への影響**: 混在にしても 7車 top1 +0.23pt(p=0.245)・tri10 −0.13pt(p=0.481) で
**劣化は検出不能**。つまり判断材料は9車側だけだった。

**9車を除外しない理由**: モデル品質はむしろ同等以上。ランダム比リフトは
top1 3.03倍（7車3.09倍）、**tri10 9.25倍（7車7.05倍）**。9車のtri10が絶対値で低いのは
組合せが210→504に増えるためで、識別力の問題ではない。

デプロイ済みモデルで実測（学習に9車を含まないまま）: 9車 top1 0.3461 / tri10 0.1908 /
ece 0.00751。推論も正常。

### 車立て依存の定数（影響度の見積もり・未修正）
- `rider_tactics.GLOBAL_TOP3_PRIOR = 3/7` は**男子39列に効いている**（escape_survival経由）。
  実測top3率は 7車0.4341（3/7=0.4286で妥当）/ **9車0.3411（3/9=0.3333が正、現行は+0.088過大）**。
  ただし直撃は実績ゼロ選手の1.45%エントリ・3.58%レースのみで、レース内相対化により
  実効の歪みは `t_escape_rel` で+0.014程度に減衰。**優先度は低い。**
- `rider_bank.WIN_PRIOR/TOP3_PRIOR` は**男子・ガールズどちらの本番モデルにも入っていない**
  （検証スクリプトからのみ参照）。現時点の実害はゼロ。
- **新たに判明**: `score_rank` は値域が 1..7 と 1..9 で異なり、`rel_score_max` の平均も
  −5.55(7車) vs −6.97(9車) とスケールがずれる。混在学習が9車で伸びなかった機序として
  最も疑わしい（未検証）。車立てで正規化すれば混在学習が有効になる可能性がある。

### 留意
9車のtop1は fold0 37.3% → fold4 31.1% と全案共通で低下傾向がある（時期・開催構成の影響と
思われるが未調査）。9車のバックテストは期間依存が強い可能性がある。
またS5で9車を扱うなら `all_trifecta_probs` が504通りになるため、210通り前提の箇所
（バケット定義・買い目点数の上限）の点検が要る。

## 4.11 車立て正規化の検証（2026-08-12）→ **不採用**

`score_rank`（値域 1..7 vs 1..9）と `rel_score_max`（平均 −5.34 vs −6.79）のスケール差が、
混在学習が9車で効かない機序ではないかという仮説を検証した。
正規化A = `(rank−1)/(N−1)` と rel を標準偏差で割る / 正規化B = 別スケーリング。

**9車テスト（n=1,396・raw7基準のMcNemar）**

| 案 | top1 | tri10 | logloss | ece |
|---|---|---|---|---|
| raw7（現行） | 33.67% | **18.34%** | 1.7409 | 0.00780 |
| raw79 | 33.74% (p=1.00) | 17.05% (p=0.051) | 1.7324 | 0.00538 |
| nA79 | 34.89% (p=0.146) | 17.62% (p=0.343) | 1.7443 | 0.00847 |
| nB79 | 34.46% (p=0.347) | 18.19% (p=0.913) | 1.7379 | 0.00758 |
| **nB7** | **35.53% (p=0.018)** | 17.62% (p=0.348) | 1.7547 | **0.01345** |

**7車テスト（n=12,286）— 正規化は全案で logloss を有意に悪化させる**

| 案 | top1 | tri10 | logloss |
|---|---|---|---|
| raw7（現行） | 44.13% | 33.59% | 1.4588 |
| nA79 | +0.19pt (p=0.409) | +0.04pt (p=0.870) | **+0.0054 (p=0.0000)** |
| nA7 | −0.22pt (p=0.325) | −0.06pt (p=0.804) | **+0.0052 (p=0.0000)** |
| nB7 | +0.08pt (p=0.729) | −0.11pt (p=0.623) | **+0.0043 (p=0.0005)** |

### 判定: 不採用
- **tri10 はどの案でも改善しない**（9車で全案マイナス、7車でも ±0.1pt 以内）。
  事前基準（tri10 が過半で改善・top1 が悪化しない）を満たす案が1つも無い
- nB7 の 9車 top1 +1.86pt(p=0.018) は本物だが、**確率の質を犠牲にしている**:
  logloss +0.0138(p=0.0046)・ece 0.0078→**0.0135**（1.7倍）。EV計算に使う確率としては劣化
- **データの90%を占める7車で logloss が有意に悪化**（p<0.001）。全体では明確にマイナス

→ 「順位の値域が違うのが原因」という仮説は、**top1 は動くが確率の質は下がる**という形で
部分的にしか支持されない。混在学習が効かない理由の説明としては不十分で、現行維持が妥当。

### 注記: seed頑健性チェックは無意味だった
3つのseedで全く同一の数値が出ている。`train_gbdt` は決定的なのでseedを変えても結果が
変わらないためで、**頑健性の根拠にはならない**（別途 fold構成を変える等が必要）。

## 4.12 Phase 4 の実装（2026-08-13）

### 4.12.1 モデル切替（タスク17）
`scripts/predict_race.py` が `is_girls_race(entries)` で分岐し、`src/model/feature_sets.py:load_for()`
がモデル・Eloを返す。**男子モデルが無いときガールズモデルへフォールバックしない**（特徴セットが
38列 vs 39列で違い、取り違えると無言で誤った推論になる。落とす方が安全）。
特徴DBも切り替える（男子は `data/keirin_men.sqlite`）。バックストレッチ補正はガールズ実測なので
男子では読まない。

### 4.12.2 予測対象の一般化（タスク18・`scripts/build_predictions.py`）
- `--include girls|men|all`（既定 girls）。`all` は1日約90R・十数会場。
- `--combos-within N`: 締切N分より先のレースは三連単210点を落とす（`combos_pruned=true`）。
  男子を全レース載せると data.json が肥大するため。締切が近づくと自動で復活する。
- `--compact`: 追加の間引き。

### 4.12.3 ダッシュボード（タスク19・`dashboard/index.html`）
トップレベルタブを **👩ガールズ / 🚴男子 / 📊成績** の3枚にした。

- **ガールズの見え方は変更なし**。ただしガールズIIFEの `allRaces` に
  `.filter(r=>r.is_girls!==false)` を足した（`--include all` の data.json で男子が
  ガールズ側に混入するため）。
- 男子は会場数・レース数が桁違いで、ガールズの「会場タブ＋レース番号タブを全部並べる」方式では
  並びきらない。**会場カード → レース一覧 → レース詳細**の3階層にした（別実装・別IIFE）。
  - 会場カード: 開催格・級班・バンク諸元・進捗バー（済/発売中/未発売）・次レース締切・注目レース
    （未確定があれば波乱確率最大、全確定なら最高配当）
  - レース一覧: レース名・◎・レースタイプ・1着確率・波乱確率／確定後は着順と払戻
- **レース詳細だけは男女で同じ形**なので `raceBlock` を共有する（`sharedRaceBlock`）。
  二重実装すると必ず片方が腐るため。EV買い目・オッズ表のセレクタ配線も `wireRaceControls(host)`
  としてホスト要素を引数に取る形へ切り出し、両方から呼ぶ。
- 進捗（済/発売中/未発売）は data.json に無い派生値。**build時刻ではなく閲覧時刻**で判定する
  （更新間隔ぶんズレるため）。

ガールズIIFEの早期returnは廃止した。`raceBlock` 等の定義は男子セクションが使うので、
データが無い日でも定義まで到達させ、`noData` フラグで `applyDate()`/`render()` だけを飛ばす。

### 4.12.4 開催格・レース名の取得（2026-08-13）
**オッズページに両方載っている**ため追加フェッチ0で取れる（`parse_race_meta`）。

| 情報 | 取得元（オッズページ内） |
|---|---|
| 開催格 | `h1.section_title` の全角表記（"松山Ｇ１"）→ 正。`span.icon_grade grN` はフォールバック |
| 開催名 | `h2.title > span.race` |
| レース名 | `div.race_title_header p.status`（"二次予選"/"Ｓ級決勝"） |

`grN` の実測対応: gr1=F2, gr2=F1, gr3=G3, gr4=G2, gr5=G1。
**全角テキストを第一情報源にする理由**: 特殊開催（全プロ記念競輪ｉｎ武雄）で `gr1` が付いており
クラスが実態とズレる例を確認したため。この方式なら未知の格（ＧＰ）も自動で拾える
（実際 `gamboo_trifecta_sample.html`＝競輪グランプリで `GP` が取れることをテストで確認）。

レース名は開催格で語彙が変わる（"選抜(2)"/"二次予選"/"ガールズ予選２"/"オリオン賞レース"）ため
**DBには生文字列で入れ、正規化は表示側**に任せる。

DB: `races` に `grade` / `race_name` を追加。既存DBは `DatasetRepo._add_missing_columns()` が
`ALTER TABLE ... ADD COLUMN`（NULL許容＝既存行を書き換えない）で無停止に補う。
適用確認: keirin.sqlite 6,597行 / keirin_men.sqlite 25,170行とも列追加のみで無傷。

## 4.13 ガールズ実測統計の男子への誤適用と分離（2026-08-13）

Phase 4 完了時点で `predict_race.py` の `is_girls` 分岐は**モデル・成績DB・バックストレッチ
の3つだけ**だった。以下4つはガールズ実測で焼き固められたまま、男子128レース/日へ無条件に
適用されていた（各モジュールが `Path(__file__).with_name(...)` で統計を直読みしていたため、
男子を通した時点で無言でガールズ値が当たる構造だった）。

| 対象 | 出典 | 男子への影響 |
|---|---|---|
| himo_adjust（紐補正） | ガールズの2着分布実測 | **表示中の三連単確率そのものを書き換えていた** |
| dev_patterns（展開6パターン） | ガールズ5,795R の分岐比 | 決まり手構造が真逆で確率が合わない |
| reference（参考フォーメーション） | ガールズ3,432R out-of-sample | 男子レースにガールズの的中率/回収率表が付いていた |
| bank_profile（統計的な有利脚質） | ガールズ6,485R | 500m会場はガールズに標本が無い |

### 実測した差（ガールズ5,795R vs 男子22,901R）
| 展開 | ガールズ | 男子 | 差 |
|---|---|---|---|
| ②◎捲り | 39〜40% | 13〜17% | **−23〜−26pt** |
| ⑥差し/マーク決着 | 13〜14% | 27〜31% | **+14〜+17pt** |
| ⑤捲り台頭 | 7〜9% | 13〜21% | +6〜+12pt |

「◎が捲って勝つ」を24pt過大評価し「◎が差し/マークに負ける」を15pt過小評価していた。
**穴狙いの判断を最も歪める方向**。バンク別決まり手も男子は差し43〜56%が支配的
（同じ400mで 男子 差48.8%/n=18,568 vs ガールズ 捲47.6%/n=5,002）。

### 対処
- `src/model/stats_profile.py` を新設し、**どの統計を使うかの決定を1箇所へ集約**。
  未実測は `None` を返しガールズ値へフォールバックしない（取り違えより非表示が安全）。
- 男子の実測を生成: `kimarite_stats_men.json`(25,228R) / `dev_pattern_stats_men.json`(22,901R)。
  生成は `analyze_gear_bank.py --db ... --emit` / `analyze_dev_patterns.py --men --emit`。
- 男子は **himo_adjust を掛けず素のPL** を出す（パラメータがガールズ実測。男子はライン決着で
  2着構造が違う。実測し直しは未着手＝A-3）。参考フォーメーションも男子では出さない。

### 副産物: augment_samples の二重実装
`augment_samples` がライン特徴に未対応で、`deploy_men.py` 側が列を挟み直していた。この二重
実装のせいで `analyze_dev_patterns` が男子モデルを使うと **31列 vs 39列** で落ちた。ライン付与を
共通関数へ取り込み、**最後に `feature_names` の並びへ揃える**ようにして挟み込み順への暗黙依存を
解消（deploy_men の旧実装と400レース・全列一致を確認済み）。

## 4.14 ダッシュボードの男女分離（2026-08-13）

「ロジックが違うなら予測もダッシュボードも独立すべき」という方針で、1ページ3タブから
**別ページ**へ変更した。

- `dashboard/index.html` = ガールズ（Phase4以前の姿へ戻した）／`dashboard/men.html` = 男子
- データも分割: `data.json` / `data_men.json`。**ガールズだけ見るときの読み込みが
  4.64MB → 0.50MB**（男子が約9割を占める）
- **男子ファイルにガールズ由来の分析セクションを入れない**（race_type_dist / calibration /
  prediction_accuracy / accuracy_history / results_history は全て keirin.sqlite 産）。
  載せると 4.13 で潰したのと同じ取り違えをUI側で再発させる
- `build_predictions` / `refresh_predictions` / `fetch_results` / `live_scheduler` を2ファイル
  対応に。確定済み予測の据え置き・結果反映・締切判定・git add はいずれも両方を見る
- 共通なのは「見た目のCSS」と「1レース詳細ブロックの描き方」だけ。抽出時に確認した通り
  レース詳細はガールズ固有の変数へ依存していない

## 4.15 Phase 5: 男子のバケット分析（2026-08-13）→ **黒字ゾーンなし**

男子7/9車 22,904レースで学習し、out-of-sample 8,017レース（2026-04-05〜08-13）の
全買い目 **191万点** で「レースタイプ×オッズ帯」の実現ROIを集計。

**結論: 男子にも安定した黒字ゾーンは無い。ガールズの結論は覆らなかった。**

### 全体像
- 全210/504点の均等買い ROI **58.0%**（控除率25%＝長期上限75%）
- favorite-longshot bias を確認。オッズ帯別ROI（全点機械買い）:

| オッズ帯 | 0-10 | 10-30 | 30-50 | 50-100 | 100-300 | 300+ |
|---|---|---|---|---|---|---|
| ROI | 69.5% | 74.5% | 74.9% | 74.6% | 71.8% | **51.5%** |

中位帯は上限75%に貼り付き、**300倍超だけ51.5%と大きく落ちる**。人気薄ほど不利で、
落差はガールズより大きい。「高配当を薄く広く」は男子では特に悪手。

### 「ROI>100%のバケット」は出るが再現しない（最重要）
同じ設定で40分違いの2回を比べた:

| バケット | 1回目 | 2回目（+1日ぶんのデータ） |
|---|---|---|
| 軸堅×100-300 @EV>=1.2 | **ROI 102.0%**（買い目2,554・的中31） | **91.4%** → 候補から消滅 |
| 標準×0-10 @EV>=1.1 | 118.2% | 108.3% |

**データが0.4%増えただけで n=2,554 の候補が消えた。** バケットを多数切って最良を
拾う行為そのものが多重比較であり、点推定のROIは採否の根拠にならない。

### レース単位ブートストラップ（`bucket_analysis.bootstrap_roi_by_race`）
買い目単位でリサンプルしてはいけない。同一レースの210〜504点は互いに強く従属する
（的中は1点だけで残りは全て外れ）ため、独立とみなすと区間が実際より遥かに狭く出る。
**リサンプルの単位はレース**にする。

| バケット | ROI | 95%CI | P(ROI>1) | 的中レース |
|---|---|---|---|---|
| 標準×0-10 @EV>=1.1 | 108.3% | [33%, 198%] | 54.8% | 7 |
| 標準×0-10 @EV>=1.15 | 118.4% | [32%, 231%] | 63.2% | 6 |
| 軸堅×100-300 @EV>=1.2 | 91.4% | [46%, 149%] | 34.2% | 27 |

いずれも的中レースが6〜27本しかなく、ROIはその数本の配当でほぼ決まる。
P(ROI>1) はコイン投げと大差ない。**実弾投入の根拠にならない。**

### 開催格・レース名で層別しても黒字ゾーンは無い（2026-08-13）
grade/race_name のバックフィル完了（**両DBとも100%**）を受けて層別した。
全点均等買いのROI（レース単位ブートストラップ95%CI）:

| 開催格 | レース | ROI | 95%CI |
|---|---|---|---|
| F2 | 4,187 | 57.7% | [52.7%, 63.3%] |
| F1 | 2,995 | 65.1% | [60.3%, 70.0%] |
| G3 | 651 | 44.2% | [37.4%, 51.8%] |
| G1 | 137 | 63.9% | [44.2%, 85.5%] |
| G2 | 47 | 43.5% | [29.8%, 59.4%] |

| レース名 | レース | ROI | 95%CI |
|---|---|---|---|
| Ｓ級予選 | 471 | 75.0% | [62.6%, 88.6%] |
| Ａ級特予選 | 339 | 66.9% | [41.4%, 104.2%] |
| Ａ級一般 | 590 | 65.2% | [55.2%, 76.7%] |
| Ａ級準決勝 | 694 | 62.3% | [51.4%, 73.8%] |
| Ａ級予選 | 728 | 42.0% | [35.2%, 49.8%] |

**上限75%を明確に超える層は存在しない。** 最高のＳ級予選ですら点推定が上限ちょうどで、
CIの上端88.6%も100%に届かない。G1が高いという直感（混戦で高配当）は支持されない
（63.9%・n=137でCIも広い）。**格による狙い撃ちは成立しない。**

### 番手の定義: 記者の並び予想のラインを正とする（2026-08-13・確定）

`parse_narabi` の `lines` が**記者の並び予想の表示そのもの**であることを実ページで確認した。
表示 `← 5先行 2追込 1追込 ／ 6追上 ／ 3押え先 7追込 4追込` → `[[5,2,1],[6],[3,7,4]]`。
区切りは `<span class="icon_p space">`。プレーンテキスト化すると区切りが消えて1本に潰れる。

**番手 = 同一ライン内の直後。ラインの最後尾・単騎は番手なし（None）。**
隊列の通し位置で判定してはいけない。ラインは連結されて並ぶので、最後尾の直後は
**次ラインの先頭＝敵**になる。

実測（男子25,155R）で加点対象の質が変わる:
| 定義 | 番手が実際に2着 | 対象 |
|---|---|---|
| 旧: 隊列の直後 | 36.4% | 23,955R |
| **新: 記者のライン** | **41.0%** | 20,006R |
| （新定義で「番手なし」＝勝者がライン最後尾/単騎） | — | 5,150R |

旧定義が誤って拾っていた相手（次ラインの先頭）の2着率は **13.3%** で、
**ランダムの16.5%を下回る**。ここへ加点すると補正が逆方向に働く。
隊列直後が真の番手と食い違うのは全選手の32.2%。

実装: `himo_adjust.marker_of(fav, narabi_pos, lines=None)`。`lines` があればそれを正とし、
無ければ従来どおり隊列の直後（**ガールズ**＝ラインの概念が無く `line_id` は45,702行中42行
＝0.1%しか無い。加えてガールズ本番38特徴モデルは旧定義で学習済みで、推論だけ変えると
train/inference skew になる）。ガールズ経路が完全に不変であることは実データで照合済み。

## 4.16 開催格・レース名のバックフィル（2026-08-13）

`scripts/backfill_grade.py`。**開催一覧ページ1枚に開催の全日程ぶんの
`<table id="tbl_day{会場}{YYYYMMDD}">` が入っている**ため、日付ごとに1フェッチではなく
**実質「開催あたり1フェッチ」**で済む（6日ぶんのテストで4フェッチ）。
レース名は `td.name[colspan]` を展開してR番号へ割り付け、class の bg_s/bg_a/bg_l で
S級/A級/ガールズも判別できる。既に埋まっている (会場,日付) はフェッチ自体を飛ばす。

実績: 790ページ取得で88,216行更新（1,085日ぶん）→ **両DBとも grade/race_name 100%**。

**gr6 = GP（競輪グランプリ）は本バックフィルで判明した。** 事前調査では「2026年の
highgrade一覧にGPの行が無く未確認」だったが、**未知のgrNを警告する実装にしていた**ため
2025-12-28〜30の平塚で検出できた。対応表（`backfill_grade.py` と `gamboo_odds.py` 両方）に追加済み。

**精度の限界**: このページの格は `icon_grade grN` のクラスのみで全角表記が無い。
特殊開催（全プロ記念競輪ｉｎ武雄）で gr1 が実態とズレる例を確認済み。1レース単位で
正確に取るならオッズページの `parse_race_meta`（h1の全角表記が第一情報源）だが、
そちらは1レース1フェッチ。G1/G2/G3とF1/F2の切り分けには本方式で足りると判断した。

## 4.17 ライン表示とライン強度の数値化（2026-08-13）

男子はラインが決着構造そのものなので、並びを1本の隊列ではなく**ライン単位**で表示し、
強さを数値化した（`src/model/line_strength.py` / men.html の `lineBlock`）。

**数値はそのレースのモデル三連単分布から直接積み上げる。** 得点合計など別の物差しを
持ち込むと表示のライン評価と買い目確率が食い違うため。
  p_win（ライン1着確率・全ライン合計=100%）/ p_12（ライン決着）/ p_top3_any（3着内に1名以上）

確定済みレースは予測を据え置く仕様なのでライン情報が付かないが、**男子は紐補正を掛けず
素のPLなので三連単分布は win_prob の関数として一意** ＝ 公開済み確率から再構成できる。
`scripts/backfill_line_strength.py` で164レース全てに後付けし、win_prob と三連単確率が
変化していないことを照合で確認済み（＝新しい予測ではなく既出確率の別の見せ方）。

### 発見: 素のPLはライン決着を **28pt 過小評価** する
| | 値 |
|---|---|
| 実測のライン決着率（男子25,155R・1-2着が同一ライン） | **56.2%** |
| 確定済み113Rでのモデル平均 | 32.1%（同レースの実測 60.2%） |
| ズレ | **−28.1pt** |

予測帯別でも 40-60% と出したレースが実測91%で決着しており、**帯域全体でズレている**。
原因はPLが各車を独立に引くため**ライン内の連携（番手が連れる）を表現できない**こと。
車立て別の実測: 5車68.1% / 6車57.5% / 7車56.5% / 8車48.0% / 9車52.0%。

→ **A-3（himo_adjust の男子パラメータ再推定）は任意の改善ではなく必要な補正**。
ガールズ版が「◎勝ち時の2着＝○を8pt過大評価」の是正だったのに対し、男子は
**ライン結束の項**が要る。番手の定義も `pos_in_line` ベース（同一 line_id かつ
pos_in_line が自分+1）に改めるべき — 現行の `_marker_of` は隊列上の直後を見るので、
別ラインの先頭がたまたま隣接した場合に番手として誤カウントする。

暫定対応として、UIに**「ライン決着確率は低めに出ます」の警告と実測基準56.2%**を併記した。
絶対値は割り引いて読み、ライン間の相対比較に使う。

## 4.18 A-3: 男子の紐補正パラメータ再推定（2026-08-13）→ **採用**

事前登録した基準（scratchpad/A3_criteria.md）で walk-forward 5fold・後半2foldを hold-out。
番手は記者の並び予想のライン基準（4.17）。

### 結果: `MEN_PARAMS = {t2: 1.15, t3: 1.4, mark: 0.5}`
| hold-out (5,498R) | PL(補正なし) | 補正後 | Δ |
|---|---|---|---|
| 三連単 log-loss | 4.7081 | **4.6043** | **−0.1038** |
| 2着 top1 的中 | 25.9% | 27.6% | +1.6pt |
| 2着 top3 的中 | 64.5% | 65.2% | +0.7pt |
| 三連単 top10 的中 | 31.0% | 33.1% | +2.1pt |

hold-out 2fold とも改善（fold3 tri10 +2.3 / fold4 +2.0）。**事前基準を全て充足＝採用。**
ガールズ（t2 1.5 / t3 1.4 / mark 0.25）とは別パラメータで、`stats_profile` から引く。

### 直感が否定された点: `mate`（勝者一般への加点）は 0 が最良
「番手が2着に来るのはラインを引いた本人の直後にいる帰結で、勝者がモデル本命かは無関係の
はず」という構造的な予想で一般形 `mate` を実装し探索したが、**最良は mate=0＝◎限定形**。
効くのは◎が勝つ場合に限られる。実測が直感を否定した。

初回探索で t3 が範囲の端(1.4)を引いたため、t2上限2.0・t3上限2.0へ広げ mark×mate の併用も
含めて再探索したが、**最適点は同一**（境界の問題ではないことを確認）。

### 重要な限界: ライン決着の絶対値はほとんど直らない
| hold-out (5,498R) | 値 |
|---|---|
| 実測のライン決着率 | 55.8% |
| PL(補正なし)の予測 | 33.8%（−22.0pt） |
| **補正後の予測** | **35.1%（−20.7pt）** |

22ptのズレのうち**縮まったのは1.3ptだけ**。順位付けの質（log-loss・top-k）は明確に上がるが、
ライン決着確率そのものは依然として大きく低い。PLが各車を独立に引く構造上、
2着の重み補正だけではライン内の連携を表現しきれない。
→ ダッシュボードの警告表示（実測基準56.2%の併記）は**外さない**。
→ 本質的に直すならモデル側（ライン単位の階層構造・2着の条件付き分布）の変更が要る。

## 4.19 (a)(b)(c) 展開予想の高度化（2026-08-14〜15）

### 前提: 展開予想に使える信号の棚卸し（analyze_development_signals.py, 25,269R）
外部サイトを取りに行く必要はなかった。記者の並び予想(脚質)＋着順＋決まり手＋S/Bマーカー＋
ライン構成が揃っており、主導権・チギリ・飛びつき・競り・連携実績はすべて自前で計算できる。
- **主導権**: ライン先頭に限っても脚質で40pt違う（先行55.2/押え先34.7/カマシ33.0/自在15.0）。
  過去B回数の四分位で層別してもなお分かれる（最少帯26pt差・最多帯19pt差）
- **チギリ**: 全体28.5%。先頭の脚質で大差（先行21.9% vs 押え先35.4%）。
  選手別「ちぎられ率」4〜57%(中央25%)・「ちぎり率」4〜56% ＝ **52ptの選手差**
- **ツッパリ**: 先行の主導権率 カマシ不在55.6% → カマシ有り50.5%（−5.1pt）
- **飛びつき(ライン外2着)43.8%**: 追込42.2/押え先19.0/自在15.7/先行13.7
- **競りは不利**: 1着4.3%/連対21.4% vs 他 14.3%/28.5%
- **連携実績は as-of で効く**: 過去3走以上のペアで、番手3着内が 過去<25%→39.1% /
  ≥50%→52.1%（**+13.0pt**）

### (a) 脚質 one-hot → 採用
以前の「脚質1列」不採用は情報ではなく**符号化**の問題。`LEG_AGGR_MEN` が
先行/押え先/カマシを全て2.0に潰していた（実測は55.2/34.7/33.0%）。
one-hot で再検証し tri10 +0.25pt(4/5fold) / top1 +0.03pt(4/5fold) で事前基準充足。
**効果量はライン特徴の1/18**と小さい。男子モデルを39→46列で再学習
（hold-out: top1 0.4338 / tri10 0.3307 / ece 0.00725）。

### (b) 主導権(B)予測モデル → 採用
| | B的中 |
|---|---|
| 記者の並び予想の先頭 | 49.4% |
| 直近B回数の最大 | 56.3% |
| **モデル** | **62.5%** |
**5/5fold で両基準線を上回る**。ガールズの展開AI(55.2%)より高い。
`backstretch_model_men.pkl`（46特徴）。`load_backstretch(is_girls)` で切替。

### (c) 展開分岐と分岐ごとの買い目
主導権の候補ごとに条件付き着順分布を作り、フォーメーションを出す。

**役割倍率**（emit_branch_stats.py, 22,913R）: 主導権者から見た役割別に
「実測シェア ÷ 素のPLのシェア」を実測。手で係数を決めない。
  1着: B番手 **1.53倍**(28.4% vs 18.6%) / 他ライン番手 **0.51倍**(10.1% vs 19.9%)
  3着: B同ライン他 **2.72倍**(19.0% vs 7.0%)

**役割倍率だけでは足りなかった（測って判明）**: 真の主導権者を与えてもライン決着
37.3% vs 実測55.1% と17.8pt低いまま。当初「P(B)の不確実性で薄まる」と考えたが誤りで、
オラクルBでも解消しなかった。役割倍率は1着・2着の重みを**別々に**掛けるだけで、
「同じラインが1-2着を占める」**同時性**を作れないため。
→ **2着の重みを「実際に1着になった車の番手」に加点する**構造へ変更（mate_of）。

**mate_boost の較正**: 実測のライン決着率に一致する値を選ぶ。初回は探索範囲の上限4.0に
**張り付いていた**（walk-forwardの5foldが全て4.5を選んだことで発覚）。境界を外して
再較正し **4.25**（n=22,910・実測56.28% vs 予測56.56%・ズレ+0.27pt）。

### out-of-sample 検証（validate_branch_calibration.py）
**fold ごとに①着順モデル②展開AI③役割倍率④mate_boost の4つ全てを学習し直す**。
モデルを全データ学習のままにすると「較正だけ分けた」ことになり検証にならない。

| fold | 実測 | 素のPL | 分岐 | PLのズレ | 分岐のズレ | LL素PL | LL分岐 |
|---|---|---|---|---|---|---|---|
| 0 | 54.4% | 34.9% | 57.0% | −19.5 | **+2.6** | 4.688 | 4.453 |
| 1 | 56.6% | 33.7% | 56.4% | −22.8 | **−0.2** | 4.666 | 4.383 |
| 2 | 53.6% | 33.1% | 55.1% | −20.5 | **+1.5** | 4.639 | 4.376 |
| 3 | 56.6% | 34.1% | 56.6% | −22.5 | **+0.0** | 4.688 | 4.405 |
| 4 | 55.4% | 33.4% | 55.8% | −22.0 | **+0.4** | 4.666 | 4.390 |

**主基準5/5・副基準5/5 で採用**。平均ズレ1.0pt / log-loss −0.2682。
主導権は予測せず真のBを条件にして測る（P(B)の不確実性を混ぜると較正の良し悪しが分離できない）。
これは分岐表示が主張する内容（「このラインが主導権を取れば〜」）と一致する条件付けである。

**A-3で「本質的に直すならモデル側の変更が要る」と書いた箇所が、これで解消した。**

### 残る課題
- 買い目は1×3×5固定・12点。分岐の確率分布の尖り方に応じた点数配分が未実装
- 三連単の**本表示**は依然として素のPL＋紐補正（ライン決着−21pt）。分岐の混合分布へ
  置き換えるなら着順モデルの出力を変えることになるので別途 walk-forward 検証が要る

## 4.20 競り（同じ位置を争うグループ）の取り込み（2026-08-15）

ユーザー指摘: 西武園2Rを「5車ライン」と表示していたが、実際は **7と1が競り**。
記者の並び予想はカッコで競りを表す（`2先行 ( 7競り 1競り ) 4追込`）。

### パーサの取りこぼし（バグ）
`parse_narabi` は `span.icon_p` から数字を拾っていたが、カッコは `bracket_open` /
`bracket_close` という**数字を持たない span** なので黙って捨てられ、`2(7 1)4 5` が
5車の直列ライン `[2,7,1,4,5]` に潰れていた。結果、番手が1人に確定して見えていた。

### 実測した影響
記録上の「勝者の番手」が2着に来る率:

| | n | 番手が2着 |
|---|---|---|
| 競りなし | — | 41.1% |
| 競りあり | — | **33.7%（−7.4pt）** |

番手を取れなかった側を番手として扱っていたことの表れ。競りは 392/25,383R = **1.54%**。

### 再フェッチが必要だった（仮説は外れた）
「連続する競り選手は1グループ」で既存データから復元できるか検証 → 曖昧な8レースを
実HTMLと突合したところ **8件すべてが2グループ**。復元は不可能で、392レースを再取得した。
結果: 385R / 952名に `seri_group` を付与。グループ数 {1:297, 2:87, 3:1}、サイズ {2:470, 3:4}。

### モデルへの投入は **不採用**（`scripts/validate_seri.py`）
案Y = 競りの選手にライン内位置を共有させる（後続は繰り上げ）＋ `ln_seri` フラグ1列（46→47列）。

事前登録した基準（**競りは全体の1.54%なので全体指標では埋もれる**ため主基準を競りレースに置いた）:
- 主基準: 競りを含むレースだけで tri10 が過半fold（3/5以上）で改善
- 副基準: 全体の tri10 が悪化しない（平均Δ ≥ −0.1pt）

| fold | 競りn | 競りtri10 基準→拡張 | Δ | 全体Δ |
|---|---|---|---|---|
| 0 | 43 | 39.53% → 41.86% | +2.33 | +0.25 |
| 1 | 37 | 48.65% → 48.65% | +0.00 | +0.29 |
| 2 | 36 | 22.22% → 19.44% | −2.78 | −0.54 |
| 3 | 35 | 42.86% → 25.71% | **−17.14** | −0.04 |
| 4 | 39 | 25.64% → 25.64% | +0.00 | +0.04 |

平均Δ 全体 +0.00pt / 競りレース **−3.52pt（1/5fold改善）** → **主基準 不充足で不採用**。

**ただしこの検証は検定力が足りない。** 競りレースはテストfoldあたり35〜43件しかなく、
tri10 の1レースが約2.7pt に相当する（fold3 の −17.14pt は実質6レース）。
「効果が無い」ことを示せたのではなく「**測れなかった**」。基準の設計時にNを見ていれば
気づけたはずで、これは検証設計側の反省。結果を見てから基準を緩めるのは筋が通らないので
判定は不採用のままとし、競りレースが積み上がった時点で再検証する。

### 検証設計のミスとその修正
最初のスクリプトは基準側にも競り補正が掛かる作りで、**位置補正ではなくフラグ1列だけを
測っていた**。競り補正と `ln_seri` 列を「モデルの feature_names に `ln_seri` があるか」だけで
一括ゲートする形に変え、片方だけ入る組み合わせが構造的に起きないようにした。

### 不採用に伴う skew の解消
一時的に推論側（`persist.py`）だけへ位置補正が入り、46列モデル（競りを直列として学習）に
食わせる状態を作ってしまっていた。上記のゲートで解消済み。デプロイ中のモデルは46列のまま、
`men_features()` と完全一致することを確認した。

### 残したもの
- `parse_narabi` の競り抽出と `narabi.seri_group` への保存（**データは今後も貯まる**）
- ダッシュボード表示: 並び予想は記者と同じくカッコで括る（`◎2(▲7 1)○4 △5 ▶ 3 ▶ 6`）。
  ライン別の強さでは「番手・内競り」「番手・外競り」と位置を明示する

### 内競り / 外競り（2026-08-15・ユーザー確認済みの慣行）
並び予想 `2( 7 1 )4 5` のカッコ内は**内→外の順**に書かれる。
- 先に書かれた **7 が内競り＝番手（2の真後ろ）を取るのが一般的**
- 後の **1 が外競り**＝7の外側に併走する

どちらが番手を取るかは走るまで確定しないが、**内が本線**として扱える。
`narabi.position` もこの順で保存しているので、`line_strength.seri_sides` はその順を使う。
`himo_adjust.marker_of` が `mem[i+1]`（＝内競り）を番手として返すのは、この慣行と一致する。
ただし競りレースで「記録上の番手が2着」が −7.4pt 落ちるのは、内が番手を取り切れない
ケースがあるため。**内に寄せた加点をどれだけ弱めるかは未検証**（要 walk-forward）。

### 確定済みレースへの後付け（`scripts/backfill_seri_json.py`）
`build_predictions` は確定済みレースの予測を辞書ごと据え置くため、競り対応を入れても
過去レースには反映されない（西武園2Rが5車の直列のままだった）。DBの `narabi.seri_group`
から `seri` と位置ラベルだけを引き直す。**p_win/p_12/p_top3_any/settle_prob は触らない**
＝公開済みの確率を書き換えないので的中実績が遡って変わらない。据え置きは辞書ごとなので
一度入れれば以後の再生成でも残る。

### 印（◎○▲△）
**モデルの1着確率順位**。`riders` は1着確率降順で格納済みなので先頭から4名に付ける。
記者の印ではないことに注意（新聞の印と食い違って当然）。並び予想と出走表の両方に出す。

## 5. タスクリスト

### Phase 0 — 前提の実測（判断材料）
1. 男子レースの試験収集（少数開催・プローブDBへ・`require_girls=False`）
2. 既存パーサが通るか確認（出走表・並び予想・結果・オッズ）
3. 並び予想の的中率を男子で測定（対ガールズ20%）
4. 級班・車立て・決まり手・開催格の分布確認
5. **判断ポイント**: ライン特徴に投資する価値があるか／設計をどう確定するか

### Phase 1 — データ基盤
6. スキーマ移行（`races.grade` 追加、必要なら級班・ライン関連）
7. 収集範囲の設計（年数・グレード・優先度）と所要時間見積り
8. 男子データの本収集（1秒間隔厳守・長時間バッチ）
9. `210` の車立て導出化

### Phase 2 — 特徴量
10. ライン構造の復元（並び予想→ライン分割）
11. ライン特徴群の設計・実装
12. 級班・開催格の特徴化
13. 既存特徴（Elo・展開・バンク）の男子データでの再計算

### Phase 3 — モデル
14. 男子モデルの学習（7車/9車の扱いを決定）
15. walk-forward評価（ガールズと同じ指標・事前基準）
16. ライン特徴の純増検証（事前登録した基準で採否）

### Phase 4 — 予測・UI
17. モデル切替（レースの `is_girls` で分岐）
18. 9車のオッズテーブル・EV・参考買い目
19. ダッシュボードの男女切替

### Phase 5 — 検証
20. バンク交互作用など既存知見の男子での再検証
21. バケット分析・ROI検証（期待は低い）

## 6. リスク

- **収集コストが最大のリスク**。男子はガールズの約10倍。1秒間隔を守ると本収集に数日〜1週間規模。
  Phase 1 の前に必要年数を絞る判断が要る（3年か、直近1年で始めるか）。
- **ROIの期待を持たない**。ガールズの結論（安定した勝てるエッジは存在しない）は男子でも覆らない見込み。
- **規律の維持**。データ量が増えるほど偶然の有意差を拾いやすい。事前基準の宣言と打ち切りを徹底する。
- **スクレイピング規約**（CLAUDE.md 課題G）。収集量が増えるので間隔と負荷に一層注意する。
